{
  "name": "lambda-function",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rimraf dist",
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc -w",
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:local": "npm run build && node dist/local-test.js",
    "test:endpoint": "npm run build && node -e \"require('./dist/local-test').runTest(process.argv[1])\" --",
    "deploy": "npm run build && zip -r function.zip dist node_modules",
    "typegen": "tsc --noemit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "aws-sdk": "^2.1692.0",
    "exceljs": "^4.3.0",
    "lambda-local": "^2.2.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.134",
    "@types/aws-sdk": "^0.0.42",
    "@types/node": "^22.14.1",
    "@types/uuid": "^9.0.8",
    "jest": "^29.7.0",
    "rimraf": "^2.7.1",
    "typescript": "^5.8.3"
  }
}
